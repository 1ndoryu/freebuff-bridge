# Evidencia E2E — freebuff-bridge (verificaciones reales)

Fecha de ejecución: **2026-08-30/31** contra el standalone de desarrollo
(`http://127.0.0.1:8788`, sesión autenticada: usuario Wan, andoryyu@gmail.com).

> Estas salidas se capturaron con `npm run cli` y `Invoke-RestMethod` reales; no son
> simulaciones. El propósito es dejar reproducibilidad de la verificación funcional del
> puente (requisito del cierre de bloque).

---

## 1. CLI `status`

```text
[freebuff-bridge] Freebuff en http://127.0.0.1:8788: autenticado
```

- Comando: `npm run cli -- status --config config.autonomo.json`
- Resultado: sesión autenticada (el origin-guard de Freebuff acepta loopback).

---

## 2. CLI `run` — modo autónomo (gestor "ninguno", Misión nativa)

- Config: `config.autonomo.json` (gestor `ninguno`, límites cortos).
- Tarea: `Responde solo con la palabra OK si puedes leer este mensaje.`
- Comando: `npm run cli -- run "<tarea>" --config config.autonomo.json`

```json
{
  "id": "5a47d15b-fcb9-4ffe-a2ea-fee6e4556a45",
  "threadId": "946c375c-bf7e-4f2f-a6b6-6be14fdc9369",
  "estado": "completada",
  "resumen": "Responde solo con la palabra OK si puedes leer este mensaje.",
  "pasos": [
    { "prompt": "Responde solo con la palabra OK si puedes leer este mensaje.",
      "criterio": "completar la tarea (fin de Misión)", "indice": 1 }
  ],
  "pasoActual": 0,
  "llamadasGestor": 1,
  "mensajes": [
    { "id": "0", "role": "user", "text": "Responde solo con la palabra OK si puedes leer este mensaje." },
    { "id": "1", "role": "assistant", "text": "OK" }
  ],
  "receipts": [],
  "iniciadaEn": "2026-08-31T00:51:11.583Z",
  "terminadaEn": "2026-08-31T00:51:26.519Z"
}
```

- Validación: el agente respondió **"OK"** (texto extraído correctamente de `parts[]`).
- El flujo autónomo terminó limpiamente en ~15 s con el SSE abortado (fix H2/H3).

---

## 3. API HTTP — servidor del puente

### 3.1 `/health`

```text
{ "ok": true, "puerto": 4299 }
```

### 3.2 Protección loopback (fix M2)

```text
GET /health con Origin: https://evil.example.com  ->  HTTP 403
```

- Origin no-loopback rechazado; sin Origin (curl/CLI) aceptado.

### 3.3 Flujo completo POST → GET (puerto 4301)

```text
LANZADA  -> {"id":"9c872cc8-7820-4e61-b60f-f6364e185a0f","estado":"lanzada"}
RESULTADO-> {"id":"e96ed301-83b5-4c56-a1a0-c4d8dcc1c80d",
             "threadId":"0d2fbe57-aece-451d-a66d-2203dd2d3077",
             "estado":"completada", ...}
```

- `POST /task` → `202 { id, estado: "lanzada" }`.
- `GET /task/:id` → resultado final `completada` con `mensajes[]`.
- Nota: en esta ejecución el mensaje del agente devolvió
  `"⚠️ All 3 unlimited-model tabs are in use. Close one or switch one to a local
  agent..."` — es una **limitación de entorno** de Freebuff (las 3 pestañas de modelo
  ilimitado están ocupadas), no un defecto del puente. La verificación de estado y
  flujo completo sigue siendo válida (thread creado, mensaje enviado, fin detectado,
  resultado estructurado).

---

## 4. Suite de tests (sin red, mocks inyectados)

```text
tests 26 · pass 26 · fail 0
```

- `npm run type-check` → OK.
- `npm run build` → OK.
- `npm test` → 26/26 (bridge 6+4 nuevos, gestor 4+3 nuevos, api-local 7, http-server 3).

Cobertura de los fixes de revisión:
- H1: `esAutonomo()` es método del port (no `instanceof`).
- H2/H3: el bridge aborta el SSE al terminar (sin stream colgado).
- H4: `signal` externo cancela la tarea.
- H5: `evaluar` no-JSON → `stop` (nunca `ok` en silencio).
- H6: SSE `fin:false` con turno `running` → fallida.
- M1: evicción del Map (TTL + límite) en el servidor HTTP.
- M2: rechazo de Origin no-loopback.

---

## Limitaciones

- La **app legítima (8787)** no se probó en esta ronda; los flujos se validaron contra la
  standalone (8788). Escenario pendiente: repetir contra 8787 (sección 8/10 del plan).
- `config.json` con secretos reales no se versiona (gitignored); solo hay placeholders
  en `config.example.json`.
